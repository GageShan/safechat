<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gageshan.safechat.mapper.ChatMapper" >


    <insert id="insert">
        insert into chats (id, sendUserId, receiveUserId, content,time)
        values (#{id},#{sendUserId},#{receiveUserId},#{content},#{time})
    </insert>
</mapper>